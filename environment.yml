name: ultratherm
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.9
  - numpy
  - scipy
  - pandas
  - biopython
  - pyyaml
  - pip
  - pip:
    # NOTE: bioconda viennarna does not work! Use PyPi
    - viennarna
